    # .env file in project root

    # Backend Environment Variables
    ENVIRONMENT=development

    # Database (PostgreSQL)
    POSTGRES_DB=askmydocs_db
    POSTGRES_USER=user
    POSTGRES_PASSWORD=password
    DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}

    # JWT Secret Key (CHANGE THIS TO A STRONG, RANDOM VALUE!)
    SECRET_KEY=ca3gAAeYOx_Q7O8IwF3KrsY4BS1RiQ3Z6qhdF9iHwaQ

    # AWS S3 Configuration (Replace with your actual S3 details)
    S3_BUCKET_NAME=your-askmydocs-bucket
    AWS_ACCESS_KEY_ID=YOUR_AWS_ACCESS_KEY_ID
    AWS_SECRET_ACCESS_KEY=YOUR_AWS_SECRET_ACCESS_KEY
    AWS_REGION=us-east-1

    # Elasticsearch Configuration
    ELASTICSEARCH_HOST=elasticsearch
    ELASTICSEARCH_PORT=9200

    # Embedding Model
    EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

    # LLM API Key (if using a commercial LLM like OpenAI)
    # OPENAI_API_KEY=YOUR_OPENAI_API_KEY # Uncomment and replace if needed

    # Frontend Environment Variables
    REACT_APP_API_URL=http://localhost:8000